syntax = "proto3";
package voter.tokenfactory.module.v1;

import "cosmos/app/v1alpha1/module.proto";

option go_package = "voter/x/tokenfactory/types";

// Module is the config object for the module.
message Module {
  option (cosmos.app.v1alpha1.module) = {go_import: "voter/x/tokenfactory"};

  // authority defines the custom module authority.
  // If not set, defaults to the governance module.
  string authority = 1;
}
